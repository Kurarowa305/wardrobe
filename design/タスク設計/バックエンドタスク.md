# バックエンド実装タスク

> 今回の実装ではテストは不要。

# BE-MS0：バックエンド基盤

- [x] BE-MS0-T01 apps/api 初期セットアップ

* **内容**: `apps/api` を TypeScript / Node.js 20 で起動可能にする
* **完了条件**:
  * `pnpm --filter api dev` でローカル起動できる
  * `tsx` ベースの開発実行ができる
  * `.env.local` 読み込みができる
* **依存**: なし

- [x] BE-MS0-T02 ディレクトリ骨格作成

* **内容**: `entry/`, `domains/`, `core/`, `clients/`, `config/` を設計資料どおりに作成
* **完了条件**:
  * `entry/lambda`
  * `entry/local`
  * `domains/{wardrobe,clothing,template,history,presign}`
  * `domains/history/stats_write`
  * `core/{errors,cursor,logging,validation,response}`
  * `clients/{dynamodb,s3}`
  * `config/env.ts`
  が存在する
* **依存**: BE-MS0-T01

- [ ] BE-MS0-T03 環境変数定義

* **内容**: `config/env.ts` で環境変数の取得・検証を行う
* **完了条件**:
  * `AWS_REGION`
  * `DDB_ENDPOINT`
  * `TABLE_NAME`
  * `S3_BUCKET`
  * `IMAGE_PUBLIC_BASE_URL`
  * `STORAGE_DRIVER`
  などを安全に取得できる
* **依存**: BE-MS0-T01, BE-MS0-T02

- [ ] BE-MS0-T04 DynamoDB クライアント実装

* **内容**: 本番AWS / ローカルDynamoDB Local の両対応クライアント
* **完了条件**:
  * `GetItem`, `PutItem`, `UpdateItem`, `Query`, `BatchGetItem`, `TransactWriteItems` を呼べる
  * `endpoint` 切替ができる
* **依存**: BE-MS0-T03

- [ ] BE-MS0-T05 S3 クライアント実装

* **内容**: presign用S3 clientを作成
* **完了条件**:
  * presigned URL 生成に必要なclientが使える
  * local/mock切替の土台がある
* **依存**: BE-MS0-T03

- [x] BE-MS0-T06 共通エラー型とエラーコード

* **内容**: API設計のエラーコードを `core/errors` に実装
* **完了条件**:
  * `VALIDATION_ERROR`
  * `INVALID_CURSOR`
  * `NOT_FOUND`
  * `CONFLICT`
  * `PAYLOAD_TOO_LARGE`
  * `UNSUPPORTED_MEDIA_TYPE`
  * `RATE_LIMITED`
  * `INTERNAL_ERROR`
  * `SERVICE_UNAVAILABLE`
  を扱える
* **依存**: BE-MS0-T02

- [ ] BE-MS0-T07 HTTPレスポンス整形

* **内容**: success / error レスポンス整形を `core/response` に集約
* **完了条件**:
  * JSONレスポンスを統一生成できる
  * エラー時に共通フォーマットで返る
* **依存**: BE-MS0-T06

- [x] BE-MS0-T08 Zodバリデーション基盤

* **内容**: path/query/body の入力検証を共通化
* **完了条件**:
  * handlerでschemaを使って検証できる
  * バリデーション失敗が `VALIDATION_ERROR` に正規化される
* **依存**: BE-MS0-T06, BE-MS0-T07

- [x] BE-MS0-T09 cursor 共通実装

* **内容**: 一覧API共通のcursor encode/decodeと整合性チェック
* **完了条件**:
  * `order` や取得条件が変わったcursorを `INVALID_CURSOR` にできる
  * `clothing` / `template` / `history` で使い回せる
* **依存**: BE-MS0-T06

- [ ] BE-MS0-T10 構造化ログ基盤

* **内容**: JSONログ出力を共通化
* **完了条件**:
  * `requestId`, `method`, `path`, `domain`, `wardrobeId`, `statusCode`, `durationMs`, `errorCode` を出力できる
* **依存**: BE-MS0-T07

- [x] BE-MS0-T11 local router 実装

* **内容**: ローカルで1つのNode.jsサーバーが全パスを受け、domain handlerへ振り分ける
* **完了条件**:
  * `/wardrobes`
  * `/wardrobes/:wardrobeId/clothing`
  * `/wardrobes/:wardrobeId/templates`
  * `/wardrobes/:wardrobeId/histories`
  * `/wardrobes/:wardrobeId/images/presign`
  をルーティングできる
* **依存**: BE-MS0-T07, BE-MS0-T08

- [ ] BE-MS0-T12 Lambda entry 実装

* **内容**: 各Lambda entryを作り、domain handlerを呼ぶアダプタにする
* **完了条件**:
  * `wardrobe_server.ts`
  * `clothing_server.ts`
  * `template_server.ts`
  * `history_server.ts`
  * `presign_server.ts`
  が存在し、localと同じusecaseを呼ぶ
* **依存**: BE-MS0-T11

---

# BE-MS1：Wardrobe

- [ ] BE-MS1-T01 Wardrobe repository 実装

* **内容**: ワードローブ作成/取得のDynamoDB repo
* **完了条件**:
  * `PutItem` でMETA作成
  * `GetItem` でMETA取得できる
* **依存**: BE-MS0-T04

- [ ] BE-MS1-T02 Wardrobe usecase 実装

* **内容**: create/get usecase
* **完了条件**:
  * createで UUIDv7 を採番する
  * getで存在しない場合 `NOT_FOUND` を返す
* **依存**: BE-MS1-T01

- [ ] BE-MS1-T03 API-01 handler 実装

* **内容**: `POST /wardrobes`
* **完了条件**:
  * `name` バリデーション
  * `wardrobeId` を返す
* **依存**: BE-MS1-T02, BE-MS0-T08

- [ ] BE-MS1-T04 API-02 handler 実装

* **内容**: `GET /wardrobes/{wardrobeId}`
* **完了条件**:
  * `name` を返す
  * 存在しない場合404
* **依存**: BE-MS1-T02

- [x] BE-MS1-T05 Wardrobe MS1 集約テスト実装

* **内容**: API-01 / API-02 の local router・Lambda adapter・CI 導線を横断検証する集約テストを追加
* **完了条件**:
  * `POST /wardrobes` と `GET /wardrobes/{wardrobeId}` の入口配線を検証できる
  * package script と CI で集約テストが継続実行される
* **依存**: BE-MS1-T03, BE-MS1-T04

---

# BE-MS2：Clothing

- [ ] BE-MS2-T01 Clothing entity / dto / schema 定義

* **内容**: clothing の request/response schema と内部型を定義
* **完了条件**:
  * `name`, `imageKey`, `status`, `wearCount`, `lastWornAt`, `createdAt`, `deletedAt` を扱える
* **依存**: BE-MS0-T08

- [x] BE-MS2-T02 Clothing key builder 実装

* **内容**: PK/SK/GSIキー生成関数
* **完了条件**:
  * base table key
  * `statusListPk`
  * `createdAt`, `wearCount`, `lastWornAt` 用ソートキー
  を生成できる
* **依存**: BE-MS2-T01

- [ ] BE-MS2-T03 Clothing repository 実装

* **内容**: get/list/create/update/delete の repo
* **完了条件**:
  * 一覧は `ACTIVE` のみ取得
  * 削除は `status=DELETED`, `deletedAt=now` の論理削除
  * GSI用属性更新を含む
* **依存**: BE-MS2-T02, BE-MS0-T04

- [ ] BE-MS2-T04 API-03 usecase/handler 実装

* **内容**: 服一覧
* **完了条件**:
  * `order`, `limit`, `cursor` を受ける
  * `nextCursor` を返す
  * `ScanIndexForward` 切替で昇降順対応
* **依存**: BE-MS2-T03, BE-MS0-T09

- [x] BE-MS2-T05 API-04 usecase/handler 実装

* **内容**: 服追加
* **完了条件**:
  * `name` 必須
  * `imageKey` 任意
  * UUIDv7採番
  * 初期 `wearCount=0`, `lastWornAt=0`, `status=ACTIVE`
* **依存**: BE-MS2-T03

- [x] BE-MS2-T06 API-05 usecase/handler 実装

* **内容**: 服詳細取得
* **完了条件**:
  * 削除済みでも取得可能
  * `status`, `wearCount`, `lastWornAt` を返す
* **依存**: BE-MS2-T03

- [x] BE-MS2-T07 API-06 usecase/handler 実装

* **内容**: 服編集
* **完了条件**:
  * `name?`, `genre?`, `imageKey?` の部分更新
  * GSI影響属性があれば整合更新
* **依存**: BE-MS2-T03

- [x] BE-MS2-T08 API-07 usecase/handler 実装

* **内容**: 服削除（論理削除）
* **完了条件**:
  * 一覧からは消える
  * 詳細取得は可能なまま
* **依存**: BE-MS2-T03

- [x] BE-MS2-T09 Clothing 単体テスト

* **内容**: repo / usecase / handler テスト
* **完了条件**:
  * 一覧ACTIVEのみ
  * 削除後の詳細取得
  * invalid cursor
  を確認
* **依存**: BE-MS2-T04, BE-MS2-T05, BE-MS2-T06, BE-MS2-T07, BE-MS2-T08

---

# BE-MS3：Template

- [ ] BE-MS3-T01 Template entity / dto / schema 定義

* **内容**: template の request/response schema と内部型を定義
* **完了条件**:
  * `clothingIds` は順序付き
  * 最大20件制約を持てる
* **依存**: BE-MS0-T08

- [ ] BE-MS3-T02 Template key builder 実装

* **内容**: PK/SK/GSIキー生成関数
* **完了条件**:
  * clothing と同様に base key / GSI key を生成できる
* **依存**: BE-MS3-T01

- [ ] BE-MS3-T03 Template repository 実装

* **内容**: get/list/create/update/delete
* **完了条件**:
  * 一覧は `ACTIVE` のみ
  * 詳細は `DELETED` も取得可能
  * `clothingIds` を保持する
* **依存**: BE-MS3-T02, BE-MS0-T04

- [x] BE-MS3-T04 Clothing BatchGet 補完 repo 実装

* **内容**: `clothingIds` から服データをまとめて取得
* **完了条件**:
  * 順序維持で返せる
  * 上限80件単位で分割取得できる
* **依存**: BE-MS2-T03, BE-MS3-T03

- [x] BE-MS3-T05 API-08 usecase/handler 実装

* **内容**: テンプレ一覧
* **完了条件**:
  * `clothingItems[]` を服データ同梱で返す
  * clothingItems は全件返却する
* **依存**: BE-MS3-T03, BE-MS3-T04, BE-MS0-T09

- [x] BE-MS3-T06 API-09 usecase/handler 実装

* **内容**: テンプレ追加
* **完了条件**:
  * `name` 必須
  * `clothingIds[]` の重複禁止
  * 参照服存在チェック
* **依存**: BE-MS3-T03, BE-MS3-T04

- [x] BE-MS3-T07 API-10 usecase/handler 実装

* **内容**: テンプレ詳細
* **完了条件**:
  * `clothingItems[]` を全件同梱
  * 削除済み服も `status=DELETED` で返せる
* **依存**: BE-MS3-T03, BE-MS3-T04

- [x] BE-MS3-T08 API-11 usecase/handler 実装

* **内容**: テンプレ編集
* **完了条件**:
  * `name?`, `clothingIds?`
  * 順序を保持
  * 参照服存在チェック
* **依存**: BE-MS3-T03, BE-MS3-T04

- [ ] BE-MS3-T09 API-12 usecase/handler 実装

* **内容**: テンプレ削除（論理削除）
* **完了条件**:
  * 一覧からは消える
  * 履歴参照では残る
* **依存**: BE-MS3-T03

- [ ] BE-MS3-T10 Template テスト

* **内容**: repo / usecase / handler テスト
* **完了条件**:
  * BatchGet補完
  * 削除済み服を含むレスポンス
  * duplicate `clothingIds` の409
  を確認
* **依存**: BE-MS3-T05, BE-MS3-T06, BE-MS3-T07, BE-MS3-T08, BE-MS3-T09

---

# BE-MS4：History（参照系）

- [ ] BE-MS4-T01 History entity / dto / schema 定義

* **内容**: history の request/response schema と内部型を定義
* **完了条件**:
  * `date`
  * `templateId | null`
  * `clothingIds`
  * `createdAt`
  を扱える
* **依存**: BE-MS0-T08

- [ ] BE-MS4-T02 History key builder 実装

* **内容**: PK/SK生成関数
* **完了条件**:
  * base key
  * `DATE#<date>#<historyId>` 用キーを生成できる
* **依存**: BE-MS4-T01

- [ ] BE-MS4-T03 History repository 実装

* **内容**: get/list/delete用基盤
* **完了条件**:
  * 日付順Query
  * `from/to/order/limit/cursor` を扱える
* **依存**: BE-MS4-T02, BE-MS0-T04

- [ ] BE-MS4-T04 History details resolver 実装

* **内容**: history + clothing BatchGet + template name 解決
* **完了条件**:
  * detail/listで使い回せる
  * clothing順序を保持できる
* **依存**: BE-MS4-T03, BE-MS2-T03, BE-MS3-T03, BE-MS3-T04

- [ ] BE-MS4-T05 API-13 usecase/handler 実装

* **内容**: 履歴一覧
* **完了条件**:
  * `date` 順
  * `name` はテンプレ入力ならテンプレ名、組み合わせ入力なら `null`
  * `clothingItems[]` を同梱して返す
* **依存**: BE-MS4-T03, BE-MS4-T04, BE-MS0-T09

- [ ] BE-MS4-T06 API-15 usecase/handler 実装

* **内容**: 履歴詳細
* **完了条件**:
  * `templateName?`
  * `clothingItems[]`
  * 削除済み服も含めて返せる
* **依存**: BE-MS4-T03, BE-MS4-T04

- [ ] BE-MS4-T07 History 参照系テスト

* **内容**: repo / usecase / handler テスト
* **完了条件**:
  * 日付範囲
  * cursor整合
  * template入力 / combination入力 の両パターン
  を確認
* **依存**: BE-MS4-T05, BE-MS4-T06

---

# BE-MS5：History（更新系）/ stats_write

- [ ] BE-MS5-T01 history/stats_write 型定義

* **内容**: `HistoryFact` などの内部型
* **完了条件**:
  * create/delete の両方で使える共通型がある
* **依存**: BE-MS4-T01

- [ ] BE-MS5-T02 history/stats_write keys 実装

* **内容**: `DATE#yyyymmdd` 等の生成
* **完了条件**:
  * clothing/template daily counter key を生成できる
* **依存**: BE-MS5-T01

- [ ] BE-MS5-T03 history/stats_write daily aggregation 実装

* **内容**: daily counter / wearCount / lastWornAt 更新ロジック
* **完了条件**:
  * create時の増分更新を表現できる
  * delete時の減分更新方針を表現できる
* **依存**: BE-MS5-T02

- [ ] BE-MS5-T04 history/stats_write recompute 実装

* **内容**: 履歴削除時の `lastWornAt` 再計算
* **完了条件**:
  * 最新日削除時に次点日をQueryできる
  * なければ 0 に落とせる
* **依存**: BE-MS5-T03, BE-MS4-T03

- [ ] BE-MS5-T05 history/stats_write transact builder 実装

* **内容**: `TransactWriteItems` 組み立て
* **完了条件**:
  * create用
  * delete用
  のitemsを生成できる
* **依存**: BE-MS5-T03, BE-MS5-T04

- [ ] BE-MS5-T06 history/stats_write guard 実装

* **内容**: `TransactWrite` 25件制限などの防御
* **完了条件**:
  * 上限超過時に明示的に失敗できる
* **依存**: BE-MS5-T05

- [ ] BE-MS5-T07 clothingStatsRepo / templateStatsRepo 実装

* **内容**: 統計更新専用repo
* **完了条件**:
  * clothing/template usecase を呼ばずに統計更新できる
* **依存**: BE-MS5-T03, BE-MS0-T04

- [ ] BE-MS5-T08 wearDailyQueryRepo 実装

* **内容**: 降順Queryで次点日を探すrepo
* **完了条件**:
  * `lastWornAt` 再計算用に使える
* **依存**: BE-MS5-T04, BE-MS0-T04

- [ ] BE-MS5-T09 API-14 usecase 実装

* **内容**: 履歴作成
* **完了条件**:
  * `templateId` と `clothingIds` は同時指定不可
  * history作成 + daily counter更新 + wearCount更新 + `lastWornAt` 更新を同一トランザクションで行う
* **依存**: BE-MS5-T05, BE-MS5-T06, BE-MS5-T07

- [ ] BE-MS5-T10 API-14 handler 実装

* **内容**: 履歴作成handler
* **完了条件**:
  * schema validation
  * `409` / `404` / `400` を仕様通り返せる
* **依存**: BE-MS5-T09, BE-MS0-T08

- [ ] BE-MS5-T11 API-16 usecase 実装

* **内容**: 履歴削除（物理）
* **完了条件**:
  * history取得後に delete transact を実行
  * daily counter減算
  * 0件ならdaily item削除
  * 必要時 `lastWornAt` 再計算
* **依存**: BE-MS5-T05, BE-MS5-T06, BE-MS5-T08

- [ ] BE-MS5-T12 API-16 handler 実装

* **内容**: 履歴削除handler
* **完了条件**:
  * `404` / `500` を仕様通り返せる
* **依存**: BE-MS5-T11, BE-MS0-T08

- [ ] BE-MS5-T13 更新系統合テスト

* **内容**: create/delete で統計整合を確認
* **完了条件**:
  * clothing wearCount
  * template wearCount
  * clothing/template daily
  * `lastWornAt` 再計算
  が期待通り
* **依存**: BE-MS5-T09, BE-MS5-T10, BE-MS5-T11, BE-MS5-T12

---

# BE-MS6：Presign

- [ ] BE-MS6-T01 presign schema / category 定義

* **内容**: presign の request/response schema と内部型を定義
* **完了条件**:
  * `contentType`
  * `category`
  * `extension?`
  を検証できる
* **依存**: BE-MS0-T08

- [ ] BE-MS6-T02 imageKey 生成ロジック

* **内容**: `category/wardrobeId/uuid.extension` 形式生成
* **完了条件**:
  * categoryごとに prefix を切れる
  * wardrobeId を含められる
* **依存**: BE-MS6-T01

- [ ] BE-MS6-T03 presign usecase 実装

* **内容**: PUT用 presigned URL 発行
* **完了条件**:
  * `imageKey`, `uploadUrl`, `method`, `expiresAt` を返す
* **依存**: BE-MS0-T05, BE-MS6-T02

- [ ] BE-MS6-T04 presign handler 実装

* **内容**: API-17
* **完了条件**:
  * wardrobe存在確認
  * validation error / unsupported media type を返せる
* **依存**: BE-MS6-T03, BE-MS1-T01

- [ ] BE-MS6-T05 presign テスト

* **内容**: repo / usecase / handler テスト
* **完了条件**:
  * MIME/type不正
  * category不正
  * 正常レスポンス
  を確認
* **依存**: BE-MS6-T03, BE-MS6-T04

---

# BE-MS7：IaC / AWS反映

- [ ] BE-MS7-T01 DynamoDB Terraform 実装

* **内容**: WardrobeTable + GSI
* **完了条件**:
  * `PK/SK`
  * 4つのGSI
  * `PITR`
  * on-demand
  が作成される
* **依存**: BE-MS2-T03, BE-MS3-T03, BE-MS4-T03

- [ ] BE-MS7-T02 S3 images Terraform 実装

* **内容**: 画像バケット + CORS
* **完了条件**:
  * `PUT/GET/HEAD` 対応
  * private bucket である
* **依存**: BE-MS0-T05

- [ ] BE-MS7-T03 Lambda Terraform 実装

* **内容**: domain別Lambda + presign
* **完了条件**:
  * `wardrobe`, `clothing`, `template`, `history`, `presign` が作成される
  * 命名規則に従う
* **依存**: BE-MS0-T12

- [ ] BE-MS7-T04 API Gateway Terraform 実装

* **内容**: HTTP API + 各path統合
* **完了条件**:
  * path prefixごとに正しいLambdaへルーティングされる
* **依存**: BE-MS7-T03

- [ ] BE-MS7-T05 IAM Terraform 実装

* **内容**: Lambda execution role
* **完了条件**:
  * presign だけS3権限
  * 他domainはDynamoDB中心
  * CloudWatch Logs 権限あり
* **依存**: BE-MS7-T03

- [ ] BE-MS7-T06 CloudFront / S3 web / images Terraform 実装

* **内容**: Web配信・画像配信基盤
* **完了条件**:
  * web配信
  * images配信
  * OAC
  が整う
* **依存**: BE-MS7-T02

- [ ] BE-MS7-T07 CIデプロイ基盤

* **内容**: GitHub Actions で Terraform plan/apply
* **完了条件**:
  * apply はCIのみ
  * concurrency 設定あり
* **依存**: BE-MS7-T01, BE-MS7-T02, BE-MS7-T03, BE-MS7-T04, BE-MS7-T05

---

# BE-MS8：品質と運用

- [ ] BE-MS8-T01 OpenAPI整備

* **内容**: `packages/openapi` に API仕様を反映
* **完了条件**:
  * API-01〜17 が定義されている
  * request/response/error が揃っている
* **依存**: BE-MS1-T03, BE-MS1-T04, BE-MS2-T04, BE-MS2-T05, BE-MS2-T06, BE-MS2-T07, BE-MS2-T08, BE-MS3-T05, BE-MS3-T06, BE-MS3-T07, BE-MS3-T08, BE-MS3-T09, BE-MS4-T05, BE-MS4-T06, BE-MS5-T10, BE-MS5-T12, BE-MS6-T04

- [ ] BE-MS8-T02 handler テスト基盤

* **内容**: local router 経由でAPIテスト
* **完了条件**:
  * supertest相当で叩ける
  * Lambda依存なしで実行できる
* **依存**: BE-MS0-T11

- [ ] BE-MS8-T03 repo テスト基盤

* **内容**: DynamoDB Local を使った統合テスト
* **完了条件**:
  * local DB に対して `Query` / `BatchGet` / `TransactWrite` を検証できる
* **依存**: BE-MS0-T04

- [ ] BE-MS8-T04 ログ/エラー監視確認

* **内容**: CloudWatch出力項目を確認
* **完了条件**:
  * `requestId` ベースで追跡できる
  * `errorCode` が出る
* **依存**: BE-MS0-T10, BE-MS7-T03, BE-MS7-T05

- [ ] BE-MS8-T05 バックエンドREADME整備

* **内容**: 起動手順、env、テスト、デプロイ方針を記載
* **完了条件**:
  * 新規参加者が `apps/api` を起動できる
* **依存**: BE-MS0-T01, BE-MS8-T02, BE-MS8-T03
