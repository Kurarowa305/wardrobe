# MS0：基盤（壊れない骨格）

- [x] MS0-T01 リポジトリ初期セットアップ（Webアプリ起動）

* **内容**: pnpm workspace 前提で `apps/web` が `pnpm dev` で起動し、トップで画面が表示できる
* **完了条件**:

  * ローカル起動できる
  * ESLint/Prettier/TypeScript の基本エラーが出ていない
* **依存**: なし

- [x] MS0-T02 ルーティング骨格（タブ＋スタック）

* **内容**: タブ4画面（ホーム/履歴/テンプレ/服）＋スタック（詳細/追加/編集/記録系）に到達できる
* **完了条件**:

  * 15画面（または現時点で定義済み全画面）に「仮でも良いので」遷移できる
  * 戻る動線が画面仕様に沿う（ホーム/一覧は戻るなし、他は戻るあり）
* **依存**: MS0-T01

- [x] MS0-T03 共通レイアウト（Header / TabBar）

* **内容**: ヘッダー（タイトル/戻る/右上アクション枠）とタブバーを共通コンポーネント化
* **完了条件**:

  * 画面ごとに「戻るあり/なし」「右上︙あり/なし」をpropsで制御できる
  * タブバーは4タブで固定表示
* **依存**: MS0-T02

- [x] MS0-T04 UI基盤（shadcn/ui or UIライブラリ導入＋トースト）

* **内容**: ボタン/入力/カード（必要なら）/トースト等の基礎導入
* **完了条件**:

  * `Button`, `Input`, `Toast` が使える
  * エラー通知をトーストで出せる
* **依存**: MS0-T01

- [x] MS0-T05 TanStack Query セットアップ（QueryClient Provider）

* **内容**: QueryClient のProvider、Devtools（任意）、エラーハンドリング方針の下地
* **完了条件**:

  * `useQuery`, `useMutation` が全画面で使える
  * 開発時にキャッシュ状態を確認できる（Devtools or console）
* **依存**: MS0-T01

- [x] MS0-T06 APIクライアント雛形（fetch wrapper）

* **内容**: `apiClient`（baseURL、エラー整形、JSON parse、タイムアウト等）を用意
* **完了条件**:

  * `GET/POST/PATCH/DELETE` の呼び出しが統一できる
  * エラー時に例外型（AppErrorなど）に正規化される
* **依存**: MS0-T01

- [x] MS0-T07 QueryKey 定義（命名規則＋集約）

* **内容**: `queryKeys` を集約し、画面で直書きしないルールにする
* **完了条件**:

  * `queryKeys.clothing.list(...)` のようにドメイン別に定義
  * invalidate対象が「キーから追える」状態
* **依存**: MS0-T05

- [x] MS0-T08 MSW 導入（起動時に自動で有効）

* **内容**: 開発環境で MSW を有効化し、APIが常に返る状態を作る
* **完了条件**:

  * `GET /health` などがモックで返る
  * ネットワークタブでモック応答が確認できる
* **依存**: MS0-T01

- [x] MS0-T09 MSW シナリオ（遅延・500・404の共通化）

* **内容**: handlerに共通の「遅延」「強制エラー」を仕込める
* **完了条件**:

  * クエリパラメータやフラグで `delay=...` / `forceError=...` が再現できる
* **依存**: MS0-T08

~~- [ ] MS0-T10 ディレクトリ責務の固定（features/components/lib）~~

* **内容**: `features/*` が画面ロジック、`components/*` が受け身、を守る雛形作り
* **完了条件**:

  * 画面は `features/<domain>/<screen>` に置く
  * ドメインAPI・型・MSW fixture の置き場所が決まっている
* **依存**: MS0-T01

---

# MS1：服（一覧→追加/編集→詳細）を完成

- [x] MS1-T01 Clothing 型定義（DTO/VM）

* **内容**: APIレスポンスDTOと画面表示用VMの型を定義
* **完了条件**:

  * `ClothingDto`, `Clothing`（VM）などが分離されている
  * `deleted` 表示に必要なフラグを含む
* **依存**: MS0-T10

- [x] MS1-T02 Clothing MSW fixtures（一覧/詳細のデータ整合）

* **内容**: 服データのfixtureを用意し、一覧・詳細で矛盾しない
* **完了条件**:

  * `fixtures/clothing.ts` にまとまっている
  * 画像あり/なし、削除済みを含む
* **依存**: MS0-T08, MS1-T01

- [x] MS1-T03 Clothing APIクライアント（CRUD＋一覧）

* **内容**: `listClothings`, `getClothing`, `createClothing`, `updateClothing`, `deleteClothing`
* **完了条件**:

  * fetch wrapperを使い、戻り値がDTO型で返る
  * エラーが統一形式で上がる
* **依存**: MS0-T06, MS1-T01

- [x] MS1-T04 Clothing MSW handlers（CRUD＋一覧＋ページング）

* **内容**: `GET /clothings?limit&cursor` 等をMSWで再現（cursorはダミーでも形は合わせる）
* **完了条件**:

  * 一覧：limit/cursorを受け、`nextCursor` を返せる
  * 詳細：存在しないIDで404を返せる
* **依存**: MS0-T08, MS1-T02, MS1-T03

- [x] MS1-T05 Clothing Query hooks（useClothingList/useClothing）

* **内容**: `useQuery` ラッパを用意し、画面はhooks経由で取得
* **完了条件**:

  * `queryKeys.clothing.list(params)` / `.detail(id)` を利用
  * staleTime等の方針が反映されている
* **依存**: MS0-T05, MS0-T07, MS1-T03

- [x] MS1-T06 Clothing Mutation hooks（create/update/delete＋invalidate）

* **内容**: create/update/delete の `useMutation` と invalidate を確定
* **完了条件**:

  * create: `clothing.list` invalidate
  * update: `clothing.detail(id)` と関連一覧 invalidate
  * delete: 一覧から消え、詳細は削除済み表示（方針に沿ってinvalidate/リダイレクト）
* **依存**: MS1-T05, MS1-T03

- [ ] MS1-T07 服一覧画面（UI＋空/読込/追加入力）

* **内容**: 一覧表示、空状態、ローディング、追加入力（“さらに読み込む”など）
* **完了条件**:

  * 服名＋画像サムネ（なければ no image）
  * タップで詳細へ
  * nextCursorがあれば追加読み込みできる
* **依存**: MS1-T05, MS0-T03, MS0-T04

- [ ] MS1-T08 服詳細画面（最小項目＋右上︙枠）

* **内容**: 服画像＋服名のみ（現方針）＋右上︙枠、削除済み表示
* **完了条件**:

  * 詳細取得エラー時の表示（404/500）
  * 削除済みは「削除済み」表記を出す
* **依存**: MS1-T05, MS0-T03

- [ ] MS1-T09 服追加画面（服名必須＋画像任意）

* **内容**: 入力、バリデーション、保存処理（画像はMS2で強化）
* **完了条件**:

  * 服名未入力で保存不可（エラーメッセージ）
  * 保存成功で詳細 or 一覧へ遷移
* **依存**: MS1-T06, MS0-T04

- [ ] MS1-T10 服編集画面（既存値反映＋更新）

* **内容**: 既存データ表示、更新処理、戻り
* **完了条件**:

  * 初期値が取れてからフォーム表示（スケルトン等）
  * 更新成功で詳細に反映される
* **依存**: MS1-T05, MS1-T06

- [ ] MS1-T11 服削除導線（︙→削除）

* **内容**: メニュー自体は簡易でもOK。削除実行と画面遷移/更新を確定
* **完了条件**:

  * 削除後：一覧に消える
  * 詳細への直リンク時に削除済み表示（仕様に合わせる）
* **依存**: MS1-T06, MS1-T08

---

# MS2：画像アップロード体験（presign前提でモック）

- [ ] MS2-T01 画像選択UI（プレビュー/クリア）

* **内容**: 服追加/編集フォームに画像選択（ファイル）とプレビュー、クリアを追加
* **完了条件**:

  * 画像を選ぶとプレビュー表示
  * 画像を外せる
* **依存**: MS1-T09, MS1-T10

- [ ] MS2-T02 presign API クライアント＋型

* **内容**: `getPresignedUrl`（PUT/POSTどちらでも設計に合わせる）のクライアント
* **完了条件**:

  * 戻り値に `uploadUrl` と `imageKey` が含まれる前提で扱える
* **依存**: MS0-T06

- [ ] MS2-T03 presign MSW handler（成功/失敗/遅延）

* **内容**: presignをMSWで再現し、エラーも出せる
* **完了条件**:

  * 成功時：`imageKey` が毎回ユニークっぽく生成される
  * 失敗時：400/500の再現ができる
* **依存**: MS0-T08, MS2-T02, MS0-T09

- [ ] MS2-T04 擬似アップロード実装（PUT先に投げる形だけ再現）

* **内容**: presign後に `fetch(uploadUrl, { method:'PUT', body:file })` を実行（MSWで受ける）
* **完了条件**:

  * アップロード中 state（ボタン無効・スピナー等）
  * 失敗時のリトライ
* **依存**: MS2-T01, MS2-T03

- [ ] MS2-T05 服作成/更新に imageKey を組み込む

* **内容**: アップロード成功後、create/updateのpayloadに `imageKey` を入れる
* **完了条件**:

  * 保存後、一覧/詳細に画像が反映される（モックでも）
* **依存**: MS2-T04, MS1-T06, MS1-T04

- [ ] MS2-T06 画像表示URLの解決（imageKey→表示URL）

* **内容**: `imageKey` から表示URLを組み立てる責務を1箇所に集約（例：`resolveImageUrl(imageKey)`）
* **完了条件**:

  * 一覧/詳細/サムネが同じ関数を使う
  * 本番でCloudFrontに差し替えやすい
* **依存**: MS1-T07, MS1-T08

---

# MS3：テンプレート（一覧→追加/編集→詳細）

- [ ] MS3-T01 Template 型定義（DTO/VM）

* **完了条件**: テンプレに含まれる服の配列（同梱データ）を型で表現できる
* **依存**: MS0-T10

- [ ] MS3-T02 Template fixtures（服同梱の整合性）

* **完了条件**: テンプレ一覧/詳細が同じ服データを参照できる
* **依存**: MS1-T02

- [ ] MS3-T03 Template APIクライアント（CRUD＋一覧/詳細）

* **依存**: MS0-T06, MS3-T01

- [ ] MS3-T04 Template MSW handlers（服同梱レスポンス必須）

* **完了条件**: `clothingIds` ではなく「服データ配列」を返す形になっている
* **依存**: MS3-T02, MS3-T03, MS0-T08

- [ ] MS3-T05 Template Query hooks＋Mutation hooks（invalidate確定）

* **依存**: MS0-T05, MS0-T07, MS3-T03

- [ ] MS3-T06 テンプレ一覧画面（サムネ＋削除済み服の扱い）

* **依存**: MS3-T05, MS0-T03

- [ ] MS3-T07 テンプレ追加/編集画面（服複数選択）

* **依存**: MS1-T05（服一覧データ活用）, MS3-T05

- [ ] MS3-T08 テンプレ詳細画面（服一覧表示＋右上︙）

* **依存**: MS3-T05

- [ ] MS3-T09 テンプレ削除導線（︙→削除）

* **依存**: MS3-T05

---

# MS4：履歴（一覧→詳細→削除）

- [ ] MS4-T01 History 型定義（DTO/VM）

* **依存**: MS0-T10

- [ ] MS4-T02 History fixtures（テンプレ/服同梱の整合）

* **依存**: MS1-T02, MS3-T02

- [ ] MS4-T03 History APIクライアント（list/detail/create/delete）

* **依存**: MS0-T06, MS4-T01

- [ ] MS4-T04 History MSW handlers（物理削除＋エラー再現）

* **依存**: MS0-T08, MS4-T02, MS4-T03

- [ ] MS4-T05 History Query hooks＋Mutation hooks（invalidate確定）

* **依存**: MS0-T05, MS0-T07, MS4-T03

- [ ] MS4-T06 履歴一覧画面（文脈表示＋空/読込）

* **依存**: MS4-T05, MS0-T03

- [ ] MS4-T07 履歴詳細画面（表示のみ）

* **依存**: MS4-T05

- [ ] MS4-T08 履歴削除導線（詳細→削除→戻り/更新）

* **依存**: MS4-T05, MS4-T07

---

# MS5：記録フロー（方法選択→テンプレ/服組合せ→作成）

- [ ] MS5-T01 記録：方法選択画面

* **依存**: MS0-T03

- [ ] MS5-T02 記録：テンプレで記録（テンプレ選択→履歴作成）

* **依存**: MS3-T05, MS4-T05

- [ ] MS5-T03 記録：服組合せで記録（複数選択→履歴作成）

* **依存**: MS1-T05, MS4-T05

- [ ] MS5-T04 記録完了後のinvalidate設計を固定（ホーム/履歴/テンプレ/服）

* **完了条件**: 「何を更新すべきか」がコード（invalidate）に落ちていて再現可能
* **依存**: MS5-T02, MS5-T03

---

# MS6：仕上げ（差し替え前の品質）

- [ ] MS6-T01 画面共通：ローディング/空状態/エラー表示の統一

* **依存**: MS1〜MS5

- [ ] MS6-T02 MSW シナリオ拡充（cursor切れ、削除済み混在、遅延）

* **依存**: MS0-T09, MS1〜MS5のhandlers

- [ ] MS6-T03 “画面×API×QueryKey×invalidate” ドキュメント更新

* **内容**: 実装と一致する表を1枚にまとめる（後でバック差し替え時の地図）
* **依存**: MS1〜MS5
